import { createClient } from '@supabase/supabase-js'; const supabaseUrl = 'https://ghlfmcalhlesuouqsaer.supabase.co'; const supabaseKey = 'sb_publishable_jZt9H6VWSznwtwBaYZLerw_oi4PY6T7'; const supabase = createClient(supabaseUrl, supabaseKey); async function run() { console.log('Finding open sessions...'); const { data: sessions } = await supabase.from('cash_sessions').select('*').eq('status', 'open').order('opened_at', { ascending: false }); console.log('Active Sessions:', sessions); if (sessions && sessions.length > 0) { const session = sessions[0]; console.log('Target session:', session.id, 'Expected:', session.expected_cash); } } run();
